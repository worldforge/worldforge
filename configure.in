dnl Process this file with autoconf to produce a configure script.
AC_INIT(wfmath/wfmath.h)

AM_INIT_AUTOMAKE(wfmath, 0.0.1)

dnl Checks for programs.
AC_PROG_CXX
AM_DISABLE_STATIC
AM_PROG_LIBTOOL

dnl Checks for libraries.
dnl Replace `main' with a function in -libs:

AM_PATH_ATLAS(0.4.3, found_atlas=yes, [
    AC_MSG_RESULT(*** Could not find libAtlas...skipping compile for libwfmath_atlas)
    found_atlas=no
]) 
AM_CONDITIONAL(HAVE_ATLAS, test $found_atlas = yes)

dnl Checks for header files.
AC_HEADER_STDC

dnl Checks for typedefs, structures, and compiler characteristics.
dnl AC_C_CONST

dnl Checks for library functions.

AC_OUTPUT(Makefile wfmath/Makefile atlas/Makefile doc/Makefile)
