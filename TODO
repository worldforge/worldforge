TODO for conftest

IMPORTANT:
 * automake/autoconf

Minor Tasks:
 * Make VALUE and EXPECT_VALUE behave nicer (loosen up on the unquoted value
   format)
 * Use a better way to do Variable::is_bool()
 * Comments aplenty

Major Tasks:
 * Add section support to getEnv() and getCmdline().

