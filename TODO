Intersect functions unimplemented:
	Intersect(RotBox<>, AxisBox<>)
	IntersectProper(RotBox<>, AxisBox<>)
        for dim != 2,
	  Contains(Polygon<>, AxisBox<>)
	  ContainsProper(Polygon<>, AxisBox<>)
	  Contains(Polygon<>, RotBox<>)
	  ContainsProper(Polygon<>, RotBox<>)
	  Intersect(Polygon<>, Polygon<>)
	  IntersectProper(Polygon<>, Polygon<>)
	  Contains(Polygon<>, Polygon<>)
	  ContainsProper(Polygon<>, Polygon<>)
        for dim != 2,3 (extremely low priority)
	  Intersect(Polygon<>, AxisBox<>)
	  IntersectProper(Polygon<>, AxisBox<>)
	  Intersect(Polygon<>, RotBox<>)
	  IntersectProper(Polygon<>, RotBox<>)
    These will be postponed until I can think of a way to write them,
    or someone needs them, whichever comes first
Add shape tests from COAL
Write rotateInverse() functions
Rip out and redo Euler angle stuff
Write an API reference for the library. Find some inline documentation tool?
Portability questions and miniball_config.h
Deal with difficulties in making certain functions friends of the
	appropriate classes
Rearrange the functions in foo_funcs.h to match the order in the class
	in foo.h
